<?php

include('../functions/login.php');

// $id = $_SESSION['id'];
// $query = executeNonQuery($connect, "SELECT * FROM member WHERE member_id = '$id'");

// $row = fetchAssoc($connect, $query);

// $name = $row['member_salut'] . " " . $row['member_first'] . " " . $row['member_last'];
